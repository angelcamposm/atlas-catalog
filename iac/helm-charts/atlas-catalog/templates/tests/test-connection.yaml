apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atlas-catalog.fullname" . }}-test-connection"
  labels:
    {{- include "atlas-catalog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atlas-catalog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
