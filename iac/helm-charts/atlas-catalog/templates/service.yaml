apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-catalog.fullname" . }}
  labels:
    {{- include "atlas-catalog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atlas-catalog.selectorLabels" . | nindent 4 }}
